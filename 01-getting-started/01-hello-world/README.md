# Hello world in Zulu

Write a function that returns "Sawubona Mhlaba!" (meaning "Hello world" in Zulu). Important: You don't have to print out the text only return it as a `string`.

Insert your code into the file `exercise.go` at the placeholder `// INSERT YOUR CODE HERE`.
